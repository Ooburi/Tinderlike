version: '3.8'

services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: rdr2
      POSTGRES_PASSWORD: 8a7cgwh4ausd_#EGsa

  api:
    build:
      context: ./API
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      - "8081:8081"
    depends_on:
      - postgres
    environment:
      - CONNECTION_STRING: "Host=postgres;Port=5432;Database=my_database;Username=rdr2;Password=8a7cgwh4ausd_#EGsa"

  bot:
    build:
      context: ./Bot
      dockerfile: Dockerfile
    depends_on:
      - postgres
    environment:
      - BOT_TOKEN=your_bot_token
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=my_database;Username=rdr2;Password=8a7cgwh4ausd_#EGsa"